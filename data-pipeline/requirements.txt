# PRECOMPUTE lane, the heavy, research-chosen SOTA engines, in the .venv-pipeline (local-only, never deployed).
# THIS is where the deep research becomes binding: every engine pinned here is documented in docs/frameworks/<tool>/
# and actually used by data-pipeline/fisuralab/stages/. The EXAMPLE engine (SIR) is numpy-only on purpose so the
# template installs instantly with no native build; a real product adds e.g. yade / pychrono / ortools / mintpy /
# torch here, each PINNED to the version it installs+verifies.
numpy==2.4.6
imageio==2.37.2         # image IO for CONTRACT 1 (image domain), PNG/JPG readers in io/image_formats.py
scipy==1.18.0           # ndimage, EDT, minimal paths (classical ladder + geometry)
scikit-image==0.26.0    # PINNED: ridge filters carry version sensitivity; synthetic-bar battery gates upgrades
scikit-learn==1.9.0     # L5 patch random forest (LBP+GLCM+HOG fusion)
PyWavelets==1.9.0       # skimage estimate_sigma dependency (NLM denoise); wavelet stage later
