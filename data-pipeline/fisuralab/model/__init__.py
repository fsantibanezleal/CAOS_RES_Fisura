"""The pure-Python analytic core, Pyodide-safe, shared by the offline stages AND the live lane (same code
path): classical primitives + ladder (classical), geometry + dual width estimators (geometry), tolerance
metrics (metrics), synthetic generators with exact ground truth (synthetic)."""
